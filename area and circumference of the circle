#include<stdio.h>
int main()
{
    int  rad,area,circum;
    float pi;
    pi=3.14;
    printf("Enter the radius: ");
    scanf("%d",&rad);
    area=2*pi*rad*rad;
    printf("Area of circle = %d\n" ,area);
    circum=2*pi*rad;
    printf("Circumference of the circle = %d",circum);
    return 0;
}
