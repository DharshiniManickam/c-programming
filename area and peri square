#include<stdio.h>
int main()
{
    int side,area,peri;
    printf("Enter the side:");
    scanf("%d",&side);
    area=side*side;
    peri=4*side;
    printf("Area of square is = %d\n",area);
    printf("Perimeter of square is %d",peri);
    return 0;
}
